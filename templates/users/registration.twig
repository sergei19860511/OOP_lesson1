{% extends 'layout.twig' %}

{% block title %}MyApp / Registration{% endblock %}
{% block content %}
    Зарегистрироваться:<br>
    {% if error %}
        <div style="color: red">Такой пользователь уже существует
        </div>
    {% endif %}
    {% if regUser %}
        Вы успешно зарегистрировались можете <a href="/users/login">Войти</a> на сайт
    {% else %} 
          <form method="post" action="/users/registration">
            <input type="text" name="login_user" placeholder ="логин"><br><br>
            <input type="text" name="pass_user" placeholder ="пароль"><br><br>
            <input type="submit" value="зарегистрироваться">
        </form>
    {% endif %}    

{% endblock %}

